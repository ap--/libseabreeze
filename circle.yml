# Test build
dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install libusb-dev

compile:
  override:
    - make lib/libseabreeze.so:
        pwd:
          SeaBreeze

# Define artifacts
general:
  artifacts:
    - "SeaBreeze/lib/libseabreeze.so"
